# FadNote Docker Environment Configuration
# Copy this to .env when running with docker-compose

# Server settings - will be exposed on this port
PORT=3000

# Storage type - overriden to 'redis' in docker-compose.yml
# For Docker, Redis is automatically configured
STORAGE_TYPE=redis

# Redis URL - automatically set to the Redis container in docker-compose.yml
# REDIS_URL=redis://redis:6379

# Filesystem storage path (only used if STORAGE_TYPE=filesystem)
# For Docker, this is automatically set to /app/data/notes
# FS_STORAGE_PATH=./data/notes

# CORS origin (required for security)
# In production, set this to your specific domain(s): https://yourdomain.com
# For development with multiple origins, you can set it specifically
# DO NOT use '*' in production - it's insecure!
CORS_ORIGIN=*

# For Cloudflare Workers deployment, these are set via wrangler.toml
# For local development, adjust values as needed
